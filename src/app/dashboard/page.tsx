import LogoutButton from "../components/auth/LogoutButton";

export default async function Dashboard() {

    return (
        <div>
            <h1>Dashboard</h1>
            <p>Welcome</p>
            <LogoutButton/>
        </div>
    );
}